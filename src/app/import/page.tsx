import { ImportStudio } from "@/components/import/ImportStudio";

export const metadata = {
  title: "Import Studio · Neural Sync Infinity Live Textbook",
  description:
    "Paste notes, upload a book photo, or drop a PDF — turn it into an interactive live chapter with AI explanations, quizzes, notes and flashcards.",
};

export default function ImportPage() {
  return <ImportStudio />;
}
